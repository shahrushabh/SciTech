<!DOCTYPE html>
<html>
 <head>
    <title>SciTech</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
      <%= link_to 'SciTech', root_path, class: "navbar navbar-brand"%>
      <div class="container">
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="nav-item <%= 'active' if params[:controller] == 'repositories' %> ">
              <%= link_to 'Home', repositories_path, class: "nav-link"%>
            </li>
            <li class="nav-item <%= 'active' if params[:controller] == 'about' %> ">
              <%= link_to 'About', about_path, class: "nav-link"%>
            </li>
          </ul>
        </div>
        <div class="dropdown">
          <button class="btn btn-dark dropdown-toggle" type="button" id="addNew" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Add New</button>
          <div class="dropdown-menu" aria-labelledby="addNew">
            <li><%= link_to 'Repository', new_repository_path, class: "dropdown-item" %></li>
          </div>
        </div>
      </div>
    </nav>

    <header>
      <div class="container text-center" style="padding-top:80px">
        <h1><small><%= @page_title ? @page_title : 'Welcome' %></small> </h1>
        <%= params[:action] %>
        <%= (params[:action] != 'new' || params[:action] != 'index') %>
      </div>
    </header>

    <section> 
      <% if params[:controller] == 'repositories' && params[:action] == 'show' %>
        <div class="row" style="width:100%">
          <div class="col-md-2" style="width:30%">
            <nav class="d-none d-md-block bg-light sidebar">
              <div class="sidebar-sticky">
                <ul class="nav flex-column">
                  <li><%= link_to 'Repository', new_repository_path, class: "dropdown-item" %></li>
                  <li><%= link_to 'Settings', new_repository_path, class: "dropdown-item" %></li>
                  <li><%= link_to 'Creators', new_repository_path, class: "dropdown-item" %></li>
                  <li><%= link_to 'Reports', new_repository_path, class: "dropdown-item" %></li>
                  <li><%= link_to 'Integrations', new_repository_path, class: "dropdown-item" %></li>
                </ul>

                <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
                  <span>Saved reports</span>
                  <a class="d-flex align-items-center text-muted" href="#">
                    <span data-feather="plus-circle"></span>
                  </a>
                </h6>
                <ul class="nav flex-column mb-2">
                  <li><%= link_to 'Your Contributions', new_repository_path, class: "dropdown-item" %></li>
                </ul>
              </div>
            </nav>
          </div>
        <% end %>
        <div class="col-md-8 page-content">
          <%= yield %>
        </div>
      </div>
    </section>

    <footer>
      <div class="footer-copyright text-center py-1">
        <p>Copyright &copy; 2018, All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
